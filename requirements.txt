# Core dependencies
Flask>=3.0.0
flask-sock>=0.7.0
flask-cors>=4.0.0
requests>=2.31.0
websocket-client>=1.6.0

# Clipboard integration (desktop client)
pyperclip>=1.8.2

# AI integration
openai>=1.0.0  # For AI summarization

# Production servers
gunicorn>=21.0.0  # Linux/Mac
gevent>=23.0.0  # WebSocket support for gunicorn
waitress>=2.1.0  # Windows

# Monitoring
prometheus-client>=0.18.0

# Optional: Redis for distributed state
redis>=5.0.0

# Optional: Desktop automation (alternative to clipboard)
pyautogui>=0.9.54
pytesseract>=0.3.10
Pillow>=10.0.0
psutil>=5.9.0

# Optional: Windows window management
pygetwindow>=0.0.9; sys_platform == 'win32'

# Development/Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
black>=23.0.0
flake8>=6.0.0
